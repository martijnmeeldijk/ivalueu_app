import 'package:flutter/material.dart';
class PhotoDiary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("You personal photos"),
      ),
    );
  }
}